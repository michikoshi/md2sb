"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs = __importStar(require("node:fs"));
const path = __importStar(require("node:path"));
const commander_1 = require("commander");
const node_1 = __importDefault(require("./../node"));
const settings = require("../../package.json");
let stdin = "";
commander_1.program
    .version(settings.version)
    .description(settings.description)
    .usage("\n\tenex2sb [file] \n\tcat hoge.enex | enex2sb")
    .arguments("[file]")
    .action(async (file) => {
    if (!process.env.GYAZO_ACCESS_TOKEN)
        return console.error("You should set env-value GYAZO_ACCESS_TOKEN to your Gyazo access token get from https://gyazo.com/oauth/applications");
    const pages = await (0, node_1.default)(fs.readFileSync(path.resolve(file)));
    console.log(JSON.stringify({ pages }, null, 2));
});
if (process.stdin.isTTY) {
    commander_1.program.parse(process.argv);
}
else {
    process.stdin.on("readable", () => {
        const chunk = process.stdin.read();
        if (chunk !== null) {
            stdin += chunk;
        }
    });
    process.stdin.on("end", async () => {
        if (!process.env.GYAZO_ACCESS_TOKEN)
            return console.error("You should set env-value GYAZO_ACCESS_TOKEN to your Gyazo access token get from https://gyazo.com/oauth/applications");
        console.log(JSON.stringify({ pages: await (0, node_1.default)(stdin) }, null, 2));
    });
}
//# sourceMappingURL=command.js.map